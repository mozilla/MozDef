[options]
esservers=http://localhost:9200
mquser=guest
mqpassword=guest
mqport=5672
mqalertserver=localhost
mqserver=localhost
alertexchange=alerts
alertqueue=mozdef.alert
mongohost=localhost
mongoport=3002
